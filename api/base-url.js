const proxy_url = alert ? "/think" : ""

export const auth = `${proxy_url}/auth`

export const info = `${proxy_url}/info/ID`

export const friend = `${proxy_url}/friend/ID`

export const record = `${proxy_url}/record`
